var src = './client/src';
var dest = './client/build';

module.exports = {
  less: {
    src: src + '/less',
    dest: dest
  },
  html: {
    src: src + '/html',
    dest: dest
  },
  js: {
    src: src + '/js',
    dest: dest
  }
}
